# CRITICAL FIX — GENRE SIGNAL IS BEING OVERRIDDEN BY SEMANTIC SCORING

The debug output shows correct scene detection:

OUTLAW COUNTRY (94% confidence)
Ecosystem lock ACTIVE
Candidate pool correctly filtered

BUT final playlist is STILL dominated by incorrect genres (e.g. indie tracks).

This proves:

👉 Genre/ecosystem filtering is NOT enforced in final ranking
👉 Semantic scoring is overpowering genre constraints
👉 “indie” tracks are scoring artificially high due to semantic similarity

---

# ROOT CAUSE

The scoring pipeline currently behaves like:

Final Score =
40% semantic
20% emotion
15% scene
10% aesthetic
10% library
5% genre

BUT in practice:

semantic + emotion + library ≈ 80–95% dominance

Genre (5%) is effectively irrelevant.

---

# REQUIRED FIX — HARD GENRE CONSTRAINT LAYER

Introduce a HARD GATE before final scoring:

## STEP 1 — Ecosystem whitelist

If scene confidence > 0.7:

ONLY allow tracks from:

Primary ecosystem genres:

* outlaw country
* country
* americana
* southern rock
* folk rock
* roots rock

ALL OTHER GENRES MUST BE EXCLUDED BEFORE SCORING.

No boosting.
No weighting.
No exceptions.

---

## STEP 2 — Genre hard penalty (NOT weight)

If a track is outside ecosystem:

Final Score = 0

NOT “lower score”

NOT “reduced boost”

→ ZERO ELIGIBILITY

---

## STEP 3 — Semantic scoring must be constrained

Semantic scoring must ONLY operate within allowed ecosystem tracks.

It cannot compare:

“rap track with high semantic match”
vs
“country track with medium semantic match”

ONLY compare:

country vs country
americana vs americana

---

## STEP 4 — FIX CURRENT FAILURE MODE

Current issue:

“indie” tracks are winning due to high semantic similarity

This must be impossible.

Indie tracks cannot even ENTER candidate pool for:

OUTLAW_COUNTRY scene.

---

## STEP 5 — DEBUG VALIDATION

Add explicit log:

“GENRE FILTER APPLIED BEFORE SCORING: YES/NO”

AND

“REJECTED TRACKS BY GENRE: [count per genre]”

AND

“FINAL CANDIDATE GENRES: [distribution]”

---

# SUCCESS CRITERIA

For prompt:

“outlaw country”

Final playlist MUST show:

≥70% country / americana ecosystem

0% rap

0% pop

0% unrelated indie (unless explicitly country-adjacent folk rock)

If this is not achieved:

Genre filter is still not enforcing hard constraints.
