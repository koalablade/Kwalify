CORE RULE

Stop trying to compute “the best playlist”.

Instead:

Build multiple valid mini-worlds, then merge them.

🧠 1. HIGH-LEVEL PIPELINE (REPLACEMENT FOR EVERYTHING CURRENT)
OLD:
input → scene detection → scoring → ranking → filtering → playlist
V3:
input
  ↓
intent decomposition (multi-axis)
  ↓
ROUTER (chooses 2–5 playlist “lanes”)
  ↓
lane-specific scoring (independent systems)
  ↓
diversity-constrained sampling inside each lane
  ↓
cross-lane interleaving mixer
  ↓
final stabilization pass
🧭 2. INTENT DECOMPOSITION (NEW CORE MODULE)

Every input must be converted into:

A. Primary intent (dominant)
“cowboy dirt roads”
“90s throwbacks”
“late night thinking”
B. Secondary intents (hidden signals)
nostalgia
driving
melancholy
freedom
energy
C. Context anchors
era (90s / mixed / modern)
environment (road / room / city / night)
motion level (static → moving fast)
🚦 3. ROUTER SYSTEM (MOST IMPORTANT CHANGE)

Instead of one scene output → you generate lanes

Each lane = a mini recommender.

Example:

Input:

“cowboy dirt roads at night”

Router outputs:

Lane 1: CORE THEME
cowboy country
dirt roads
americana
Lane 2: EMOTIONAL SHADOW
nostalgia
loneliness
reflective rural
Lane 3: MOTION LAYER
driving
highway
open road energy
Lane 4: CONTRAST LANE (CRITICAL)
indie folk
alt acoustic
cinematic soft rock
🧠 4. LANE SCORING SYSTEM (ISOLATED PER LANE)

Each lane gets its own scoring bias:

Base score:
score = embedding_similarity
Lane modifiers:
Cowboy lane:
+country affinity
+acoustic instruments
+warm timbre boost
Nostalgia lane:
+older masters bias (pre-2010)
+analog texture boost
Driving lane:
+tempo stability
+rhythm consistency
Contrast lane:
+novelty boost (VERY important)
+genre distance from main lane
🎯 5. CRITICAL CHANGE: NO GLOBAL RANKING

❌ DO NOT merge all tracks then rank

Instead:

Each lane produces:

Top N candidates (independent ranking)

Then you merge later.

🔀 6. INTERLEAVING ENGINE (THIS FIXES GENRE COLLAPSE)

Instead of sorting everything together:

Use round-robin sampling:

Example output structure:

1. cowboy lane
2. nostalgia lane
3. driving lane
4. contrast lane
5. cowboy lane
6. nostalgia lane
...

BUT with weights:

core lane: 40%
emotional lane: 25%
motion lane: 20%
contrast lane: 15%
🧱 7. DIVERSITY IS NOW STRUCTURAL (NOT A PATCH)

Instead of:

genre caps
penalties
post-filter injection

You enforce:

HARD RULE:

No lane may exceed:

35% same genre
50% same energy band
60% same era

So collapse becomes mathematically impossible inside a lane

🧊 8. FALLBACK SYSTEM (FIXED PROPERLY)

If intent is unclear:

DO NOT return “generic mood”

Instead:

Create default routing ensemble:

Lane 1: safe mainstream coherence
Lane 2: nostalgia injection
Lane 3: discovery / novelty lane
Lane 4: low-energy ambient fallback

So fallback is STILL multi-lane.

🧠 9. SCENE SYSTEM REPLACEMENT

❌ remove:

single scene detection
resolveSemanticScene
confidence fallback

✔ replace with:

“Scene Influence Map”

Instead of:

scene = “driving”

You output:

driving → 0.35
nostalgia → 0.25
night → 0.20
freedom → 0.20

Scenes become:

soft forces, not labels

🎛️ 10. FINAL PLAYLIST BUILDER

Instead of ranking → filtering → trimming

You do:

Step 1:

Generate lane outputs independently

Step 2:

Normalize per-lane distributions

Step 3:

Interleave with weighted sampling

Step 4:

Apply only ONE final constraint:

no duplicate tracks
max 2 consecutive same genre
💣 WHY V3 ACTUALLY FIXES YOUR SYSTEM

Because it removes the core failure mode you keep hitting:

❌ Old system assumption:

“One correct interpretation of the prompt exists”

✔ V3 assumption:

“Multiple valid interpretations must coexist”

That is exactly why:

90s works
cowboy works
late night works
driving works
mixed prompts stop breaking everything
🧠 WHAT YOU GET AFTER V3

Instead of:

indie collapse
generic fallback
scene failure
genre dominance

You get:

✔ stable diversity
✔ prompt robustness
✔ multi-mood playlists
✔ era coherence without locking
✔ “feels curated” behavior