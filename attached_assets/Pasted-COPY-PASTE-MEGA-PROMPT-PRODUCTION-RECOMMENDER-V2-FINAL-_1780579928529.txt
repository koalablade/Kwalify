COPY/PASTE MEGA PROMPT — “PRODUCTION RECOMMENDER V2 (FINAL LOCK)”

Use this in Replit exactly as-is.

🧠 SYSTEM ROLE

You are now implementing a Production-Grade Music Recommendation Engine (V2 FINAL ARCHITECTURE LOCK).

You MUST stop all prior iterative tuning systems (V9, V10, V11, experimental scoring variants).

You are not tuning a model anymore — you are replacing the architecture.

🚨 HARD RESET INSTRUCTIONS

Before making any changes:

DELETE or fully disable:
ecosystem lock system
pre-filter genre removal system
scene-based hard penalties
scoring pool caps
confidence gating logic
hard genre suppression
“anti-genre” multipliers
multi-layer scoring weight experiments (6-channel systems)
ENSURE:
NO track is ever removed due to genre, scene, or confidence
ALL tracks enter scoring pool if metadata is valid
🧱 NEW ARCHITECTURE (V2 FINAL)
1. INTENT PARSER (NEW CORE INPUT LAYER)

Replace scene-first logic with structured intent extraction:

type UserIntent = {
  era: "60s" | "70s" | "80s" | "90s" | "00s" | "10s" | "20s" | "any";
  energy: number; // 0.0 - 1.0
  mood: string[];
  activity: "driving" | "working" | "party" | "chill" | "focus" | "nostalgia" | "walking" | "unknown";
  vibeTags: string[];
};

👉 Scene detection becomes OPTIONAL metadata only, never a driver.

2. CANDIDATE POOL (NO FILTERING)

DO NOT REMOVE TRACKS.

const pool = allTracks.filter(t =>
  t.audioFeatures &&
  t.embedding != null
);

No:

genre filtering ❌
scene filtering ❌
ecosystem filtering ❌
pre-score caps ❌

EVERYTHING competes fairly.

3. TRIPLE SIGNAL SCORING MODEL (CORE ENGINE)

Replace all scoring systems with this:

A. Semantic Relevance (R)
R = cosineSimilarity(track.embedding, intent.embedding)
B. Vibe Match (V)

Combination of:

energy alignment
valence / mood match
tempo similarity
V =
  (energyMatch * 0.4) +
  (moodAlignment * 0.3) +
  (audioFeatureSimilarity * 0.3)
C. Context Match (C)

Soft signals only:

era match
activity match
weak scene affinity (if exists)
C =
  (eraMatch * 0.5) +
  (activityMatch * 0.3) +
  (sceneSoftAffinity * 0.2)
4. FINAL SCORING FUNCTION (SIMPLE + STABLE)
finalScore =
  (0.45 * R) +
  (0.35 * V) +
  (0.20 * C);

NO OTHER SCORING SYSTEMS EXIST.

5. DIVERSITY ENGINE (CRITICAL STABILITY LAYER)

After ranking only (NOT filtering):

Genre repetition control:
if (genreStreak > 3) score *= 0.70;
if (genreStreak > 5) score *= 0.55;
Era repetition control:
if (eraStreak > 4) score *= 0.65;
Artist repetition control:
if (artistRepeat > 1) score *= 0.40;
6. BUCKETED SELECTION (ANTI-COLLAPSE SYSTEM)

Instead of picking top N directly:

const buckets = groupBy(tracks, ["genre", "era"]);

Then build playlist:

25% top genre cluster
25% secondary genre cluster
25% era match cluster
25% discovery/random high-score

This prevents:

“everything becomes indie / one genre collapse”

7. SCENE SYSTEM (SOFT CONTEXT ONLY)

Scenes are NEVER used for:

filtering
gating
scoring dominance

Only allowed usage:

scene → modifies:
- transition smoothness
- energy slope
- ordering preference

Max influence:

sceneWeight <= 0.10
8. SEQUENCER (FLOW ENGINE)

After selection, reorder for listening experience:

smoothness =
  tempoDelta +
  energyCurve +
  keyCompatibility +
  vibeContinuity;

Then optimize playlist order.

9. ERA MODEL (NEW CORE FEATURE)

Each track has era embedding:

90s = warm / analog / guitar-heavy / mid-energy
00s = compressed pop / early digital
10s = EDM / trap / loud mastering
20s = hybrid / algorithmic / genre-blended

Use this in:

Context score
bucket grouping
sequencing
10. ABSOLUTE RULES (NON-NEGOTIABLE)
NEVER remove tracks based on genre
NEVER use confidence gating
NEVER use ecosystem locks
NEVER use hard scene penalties
NEVER collapse candidate pool before scoring
NEVER allow a single genre to exceed 60% dominance
ALWAYS ensure at least 3 genres per playlist when available
11. OUTPUT GUARANTEE

System must always return:

diverse playlist
multi-era distribution (when possible)
multi-genre distribution
smooth energy curve

Even if:

input is “90s throwbacks”
input is “cowboy dirt roads”
input is “rave”
input is “chill study”

System must generalise gracefully.

🧠 DESIGN GOAL

You are building:

A Spotify-grade recommender that NEVER collapses into one genre cluster and NEVER over-filters.

🚀 FINAL INSTRUCTION

After implementing:

ensure build passes
ensure no scoring layer depends on filtering
ensure all diversity logic is post-score only
ensure deterministic output stability