D. Activity Vector
driving
studying
walking
partying
cleaning
working

Must be explicitly inferred even if subtle

3. SCENE SYSTEM (CRITICAL CHANGE)

Replace:
❌ resolveSemanticScene(): single output

WITH:
✔ resolveSceneDistribution()

Output:

[
{ scene: "driving", weight: 0.35 },
{ scene: "nostalgia", weight: 0.25 },
{ scene: "late_night_thinking", weight: 0.20 },
{ scene: "empty_roads", weight: 0.20 }
]

If confidence is low:
DO NOT fallback → expand distribution breadth instead

4. SCORING MODEL (V2 FORMULA)

Replace all scoring with:

FINAL_SCORE =
(
EmbeddingSimilarity × 0.25 +
SceneAffinity × 0.25 +
EmotionMatch × 0.20 +
EraMatch × 0.15 +
ActivityMatch × 0.10 +
NoveltyBoost × 0.05
)

IMPORTANT:

NO genre weight in primary scoring
genre only used as soft diversity signal AFTER scoring
5. GENRE SYSTEM (ANTI-COLLAPSE RULE)

Genre is NOT a scoring factor.

Instead:

Apply post-processing constraint:

max 18% of playlist from any single genre
enforce rolling diversity window (last 12 tracks)
inject counter-genre every 4–6 tracks automatically

Example:
if too much indie → inject alt rock / country / electronic contrast

6. POOL SELECTION (FIX CRITICAL FAILURE)

DO NOT reduce 9,000 → 1,000 early.

Instead:

maintain full pool during scoring
apply only lightweight ANN pre-filter (max 40% reduction)
diversity sampling occurs AFTER scoring
7. PLAYLIST BUILDER (NEW SYSTEM)

Replace greedy ranking with:

Stratified Sampling Engine

Divide ranked tracks into buckets:

Top emotional match
High scene match
High novelty
Era-balanced
Genre-diverse

Then sample:

40% from top match
30% from secondary match
20% from novelty/diversity
10% random exploration (bounded)

8. FALLBACK SYSTEM (CRITICAL FIX)

If:

no scene matched
low confidence
weak embedding similarity

DO NOT default to "generic mood scoring"

Instead:

Generate hybrid fallback:

broaden scene vector
increase novelty weight
expand era distribution
reduce embedding dominance
9. DEBUG OUTPUT (REQUIRED)

Always output:

scene distribution
emotion vector
activity vector
era vector
diversity entropy score
genre concentration score
sampling breakdown
10. FINAL GOAL

System must successfully interpret:

“90s throwbacks”
“cowboy dirt roads”
“late night thinking about life”
“driving with windows down”
“cleaning my room nostalgic”
“gym hype but sad”

WITHOUT collapsing into:

indie-only playlists
single scene output
embedding monoculture
END SYSTEM