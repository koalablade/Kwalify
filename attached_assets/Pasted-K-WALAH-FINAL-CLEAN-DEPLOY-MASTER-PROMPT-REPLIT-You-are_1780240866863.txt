K_WALAH FINAL CLEAN + DEPLOY MASTER PROMPT (REPLIT)

You are working on an existing full-stack production project:
K_WALAH — Emotional AI Spotify DJ

Stack:

React + Vite frontend
Express 5 backend
PostgreSQL database
Spotify API integration
TypeScript monorepo
🎯 OBJECTIVE

Prepare this project for production deployment on Render + GitHub.

This is NOT a rewrite.

You must:

clean the repo
remove unnecessary files
ensure build works
ensure deployment works
generate documentation for a non-developer
make the project GitHub-safe and Render-ready
🧹 PHASE 1 — SAFE CLEANUP (DO NOT BREAK APP)

Remove ONLY build/system junk:

Delete if present:
node_modules/
dist/
build/
.vite/
.cache/
coverage/
logs/
any temporary or generated files
NEVER delete:
src/
lib/
routes/
package.json
tsconfig.json
vite.config.*
database schema files
🔐 PHASE 2 — SECURITY CLEANUP

Ensure NO secrets are included anywhere.

MUST DO:
Remove all .env files from repo
Ensure no API keys exist in code
Ensure no tokens or credentials are hardcoded
CREATE:

.env.example containing:

DATABASE_URL=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=
SESSION_SECRET=
FRONTEND_URL=
NODE_ENV=

(All values must be blank placeholders only)

📦 PHASE 3 — GIT + GITHUB READINESS

Create:

.gitignore

Must include:

node_modules
dist
build
.vite
.cache
.env
.DS_Store
*.log

Ensure repository size is GitHub-safe (<100MB recommended total).

🚀 PHASE 4 — BUILD VALIDATION

Run and confirm:

Backend:
compiles successfully
no missing imports
server starts without errors
Frontend:
builds successfully with Vite
no TypeScript errors

Fix anything that prevents build success.

DO NOT redesign architecture.

📄 PHASE 5 — CREATE DEPLOYMENT DOCUMENTATION (IMPORTANT)

Generate the following files:

1. README_DEPLOYMENT.md

Must include:

PROJECT OVERVIEW
What K_WALAH is
Tech stack
Architecture diagram (text-based)
RENDER DEPLOYMENT GUIDE (STEP BY STEP)

Explain for beginners:

how to create Render account
how to connect GitHub repo
how to create Web Service
build command
start command
environment variables setup
PostgreSQL setup on Render
ENVIRONMENT VARIABLES TABLE

Include:

variable name
purpose
where to get it
required or optional
SPOTIFY SETUP GUIDE
how to create Spotify developer app
how to get client ID/secret
how to set redirect URI
required scopes
TROUBLESHOOTING
login issues
redirect URI errors
database issues
missing env variables
build failures
2. README_ENVIRONMENT_VARIABLES.md

Explain:

each env variable in detail
example values
where to find them
common mistakes
3. README_PROJECT_STRUCTURE.md

Explain:

folder structure
backend vs frontend separation
key files
what NOT to edit
4. README_RENDER_CHECKLIST.md

Include a checklist:

□ GitHub repo connected
□ Render web service created
□ PostgreSQL attached
□ Env variables set
□ Spotify redirect URI added
□ Build successful
□ Server starts
□ Login works
□ Sync works
□ Generate works
□ Playlist creation works

5. README_FIRST_RUN.md

Step-by-step:

open Render URL
login via Spotify
sync library
generate playlist
verify playlist in Spotify
check history page
⚠️ PHASE 6 — DEPLOYMENT SAFETY RULES

Ensure:

no secrets in repo
no API keys exposed
no debug logs in production
CORS configured safely
session cookies enabled
database connection stable
🧠 PHASE 7 — FINAL OUTPUT

At the end, provide:

REPORT:
What files were deleted
What files were added
Any build fixes applied
Any risks remaining
CONFIRM:
GitHub upload ready (YES/NO)
Render deploy ready (YES/NO)
Anything still required manually
🚨 IMPORTANT RULES
DO NOT redesign the system
DO NOT remove features
DO NOT simplify architecture
ONLY clean, stabilize, document
Keep everything production-safe
🎯 GOAL

A clean, deployable, beginner-friendly version of K_WALAH that can:

be pushed to GitHub without errors
be deployed on Render immediately
work without developer knowledge
include full documentation for setup
💡 WHY THIS WORKS

This forces Replit to act like:

a DevOps engineer
a documentation writer
a deployment assistant

NOT a “rewrite the project” agent