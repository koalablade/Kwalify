Run ONE real playlist generation (e.g. “Indie Summertime Drive”) using the existing system.

Trace and output:

1. Input:
- prompt used
- user context (if any)

2. Data source discovery:
- where candidate tracks come from (exact file/function)
- database tables queried
- whether data is empty or populated

3. Candidate pool:
- exact number of tracks returned
- sample 10 track IDs + artists
- confirm whether pool is real or fallback

4. Scoring pipeline:
For each stage, show:
- function name
- inputs
- outputs
- transformations applied

5. Classification:
For each track:
- genrePrimary
- subgenre
- confidence
- taxonomyHit (true/false)
- fallback path used (if any)

6. Final selection:
- final playlist tracks
- selection reasoning (why each track was chosen)
- any duplicate bias or repetition patterns

7. Data integrity check:
- missing fields
- undefined values
- mismatched IDs
- silent fallback usage

────────────────────────────────────
PHASE 2 — ROOT CAUSE IDENTIFICATION
────────────────────────────────────

After trace, identify ONLY real issues:

Classify each issue as:
- DATA ISSUE (missing or empty data source)
- LOGIC ISSUE (incorrect scoring/classification)
- FLOW ISSUE (wrong ordering or bypassed steps)
- FALLBACK ISSUE (silent degradation)
- INTEGRATION ISSUE (Spotify/DB mismatch)

For each issue provide:
- exact file/function responsible
- why it happens
- evidence from trace

Do NOT guess — only use observed runtime behaviour.

────────────────────────────────────
PHASE 3 — MINIMAL FIXES ONLY
────────────────────────────────────

Apply ONLY fixes that are directly justified by the audit.

Rules:
- no rewrites
- no redesign
- no new systems
- no mock data
- no feature additions

Fix only:
- broken logic
- incorrect thresholds
- invalid fallbacks
- missing guards
- incorrect data mapping

Each fix must include:
- file name
- line/function
- exact change description
- reason tied to audit evidence

────────────────────────────────────
PHASE 4 — VALIDATION RUN
────────────────────────────────────

Re-run the SAME playlist generation after fixes.

Confirm:
- candidate pool correctness
- classification stability
- no silent fallback usage
- no undefined genres
- consistent track selection logic

Output:
- before vs after comparison
- remaining issues (if any)