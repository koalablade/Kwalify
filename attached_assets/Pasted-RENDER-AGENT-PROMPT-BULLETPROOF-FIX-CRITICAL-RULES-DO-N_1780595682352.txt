RENDER AGENT PROMPT (BULLETPROOF FIX)
⚠️ CRITICAL RULES
DO NOT create V4
DO NOT redesign architecture
DO NOT change core scoring logic unless it is clearly incorrect
DO NOT remove V11 or V3.1
DO NOT invent new pipelines
ONLY fix correctness, wiring, and observability
🎯 OBJECTIVE

Fix the system so that:

1. The debug panel reflects the ACTUAL playlist decision logic (V3.1)
2. V11 is clearly labeled as a pre-processing / candidate scoring layer
3. No mismatch exists between:
what is shown in UI
what actually generates the playlist
4. The system becomes fully traceable end-to-end
🔍 ROOT PROBLEM TO FIX

Currently:

_debug = V11 hybrid scoring output
v3Diagnostics = V3.1 pipeline output (ignored)
frontend reads ONLY _debug

Result:
👉 UI lies about how playlist is generated

🛠️ REQUIRED FIXES
1. Fix Debug Panel Source of Truth
CHANGE FRONTEND ONLY

In:

app.js (buildDebugPanel)
REPLACE LOGIC:

❌ CURRENT:

const dbg = result._debug;

✅ NEW:

const dbg = result.v3Diagnostics ?? result._debug;
2. Merge Diagnostics Properly (Backend)

In:

generation.controller.ts
CREATE unified debug object:
result.debug = {
  v11: pipeline.scoringDiagnostics,
  v3: pipeline.scoringDiagnostics.v3Pipeline,
  activePipeline: "v3.1_unified_routing"
};
KEEP:
result._debug (legacy compatibility)
ADD:
result.debug (new truth layer)
3. Fix Naming Confusion (CRITICAL)
V11 fields MUST be renamed in output:

Replace misleading UI labels:

OLD LABEL	NEW LABEL
Detected Scene	V11 Pre-Scene Signal
Scoring Weights	V11 Candidate Weights
Top Scored Tracks	V11 Ranked Candidates
Candidate Pool	V11 Filtered Pool
4. Expose V3.1 as PRIMARY diagnostics

Ensure v3Pipeline includes:

MUST EXIST:
lane definitions
lane weights
lane selection counts
cluster breakdown
diversity rejection reasons
final selection trace
5. Add FINAL DECISION TRACE (CRITICAL)

In V3.1 output, add:

finalDecisionTrace: {
  forEachTrack: {
    enteredLane: string,
    laneScore: number,
    diversityPenalty: number,
    clusterId: string,
    selected: boolean,
    rejectionReason: string | null
  }
}
6. Fix “No scene matched” bug propagation

If V11 semantic resolution fails:

DO NOT allow it to bias V3.1 indirectly

Instead:

if (!semanticResolution) {
  semanticResolution = {
    sceneId: null,
    confidence: 0,
    fallback: true
  };
}

And explicitly mark:

sceneStatus: "fallback"
7. Ensure V3.1 is NOT dependent on V11 ranking

VERIFY:

V3.1 input = scoring.sorted
BUT NOT dependent on:
V11 top scoring order
V11 weights
V11 final ranking

If any dependency exists → remove it.

8. Add SYSTEM HEALTH CHECK LOG

At end of generation:

systemDiagnostics: {
  v11UsedFor: "candidateGeneration",
  v3UsedFor: "finalSelection",
  debugPanelAligned: true,
  pipelineConsistency: "OK"
}
🧪 ACCEPTANCE TESTS

System is ONLY correct if:

UI shows:
lanes (V3.1)
diversity decisions (V3.1)
final track selection reasons (V3.1)
AND

V11 only appears as:

candidate scoring layer
NOT decision layer
🔥 FINAL GOAL

After fix:

You should be able to answer:

“Why did this track get selected?”

WITHOUT referencing V11 at all.

Only V3.1 should explain final output.

🚨 NON-NEGOTIABLE OUTCOME

If V11 is still driving UI explanations after this fix:

❌ system is still broken

If V3.1 is not visible in debug panel:

❌ system is still partially blind

If both are aligned:

✅ system is production-grade and debuggable