1. SCENE DETECTION PURPOSE (REDEFINED)

Scene detection is NOT:

emotional interpretation
storytelling
vibe invention
aesthetic imagination

Scene detection IS:

mapping user language → fixed real-world listening context

🧱 2. INPUT SIGNAL PRIORITY (VERY IMPORTANT)

When detecting scene, prioritise in this exact order:

Tier 1 — Hard Context Words (highest weight)

These decide scene almost entirely:

dirt road
country road
night drive
sunset drive
truck
highway
neon city
small town
festival
workout
study
Tier 2 — Directional Modifiers

These adjust confidence, NOT scene type:

calm
chill
emotional
hype
nostalgic
warm
dark
soft
energetic
Tier 3 — Decorative Language (IGNORE FOR CLASSIFICATION)

These MUST NOT affect scene:

cinematic
aesthetic
vibes
beautiful
dreamy
aesthetic
emotional journey
🎯 3. SCENE DECISION RULE (NEW CORE LOGIC)

You MUST follow:

Step 1 — Extract dominant noun context

Example:

“country dirt road sunset drive”

Dominant nouns:

country
dirt road
sunset
drive

→ classify as ROAD / OUTDOOR

Step 2 — Pick ONLY ONE category

Never combine:

❌ “country + sunset + chill”
✔ “country roads”

❌ “night drive + neon city”
✔ “night drive”

Step 3 — Apply modifier only to confidence

Modifiers do NOT change scene.

They only adjust confidence:

Example:

“chill country road sunset”
→ scene = country roads
→ confidence = slightly reduced if ambiguous wording exists
⚖️ 4. CONFLICT RESOLUTION RULE

When multiple scenes appear:

Use priority order:

Road / Outdoor
Urban / Night
Nostalgia / Warm
Chill / Focus
Energy / Motion

Never average them.

Never blend them.

🧠 5. CONFIDENCE SYSTEM (FIXED LOGIC)

Confidence is based ONLY on clarity of anchors:

0.90–1.00
exact match (“dirt road drive”, “night drive”)
0.75–0.89
strong partial match (“country sunset road”)
0.60–0.74
mixed signals but still category clear
<0.60
fallback scene required
🚫 6. HARD ANTI-RULES (CRITICAL STABILITY)

NEVER:

infer mood into new scene types
create hybrid scenes
output multiple scenes
use genre to determine scene
let scoring influence scene detection
adjust scene based on past outputs

Scene detection must be:

stateless and deterministic

🧪 7. FALLBACK RULE (VERY IMPORTANT)

If uncertain:

Default ONLY to:

country roads
night drive
calm focus

No exceptions.

No creativity.

📊 8. DEBUG OUTPUT EXTENSION (OPTIONAL)

Add:

anchor_words_matched: []
modifiers_detected: []
ignored_tokens: []

This helps you see exactly why classification happened.

🧷 9. DESIGN PRINCIPLE (FINAL)

This system behaves like:

a strict labeling function, not an interpretation model

Its job is to remove ambiguity BEFORE scoring starts.

🔥 WHY THIS FIXES YOUR REAL ISSUE

Your system previously failed because:

scene detection was too “creative”
scoring then tried to compensate
ecosystem logic over-corrected again
you got feedback loops

Now:

scene detection becomes a clean input signal, not a decision engine

🧩 FINAL STACK YOU NOW HAVE
V9 → stable scoring system (no ecosystem chaos)
V9.1 → stable scene classification (no drift)
V9.2 → precision rules (no ambiguity leakage)
🧠 IMPORTANT TRUTH (this is the last missing insight)

At this point:

You do NOT need more prompts.

You need:

better embeddings / tagging / track metadata quality

Because once:

scene is stable
scoring is stable

…the only remaining errors come from:

imperfect track representation