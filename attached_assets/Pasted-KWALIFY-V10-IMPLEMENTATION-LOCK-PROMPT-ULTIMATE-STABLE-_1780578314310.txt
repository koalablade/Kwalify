KWALIFY V10 — IMPLEMENTATION LOCK PROMPT (ULTIMATE STABLE VERSION)

This is designed to work across:

eras (60s → 2020s)
moods (happy, sad, nostalgic, angry, calm)
scenes (road trips, clubs, workouts, studying, party, rain, summer, winter)
vague inputs (“vibes”, “something chill”, “like 90s stuff”, etc.)

Without collapsing into genre lock or generic fallback.

🚨 HARD RULES (NON-NEGOTIABLE)
❌ NEVER DO:
never remove tracks before scoring
never hard-filter by genre
never return “no scene matched”
never fall back to generic mood mode
never use confidence thresholds to switch logic paths
never reduce candidate pool below scoring stage
✅ ALWAYS DO:
keep full candidate pool
score everything
rank everything
fix diversity AFTER ranking only
1. SCENE SYSTEM (VECTORED, NEVER BINARY)
Replace ALL scene detection with this:
Scene output MUST ALWAYS be:
sceneVector = [
  { id: "road_trip", weight: 0.40 },
  { id: "nostalgia_90s", weight: 0.35 },
  { id: "warm_evening", weight: 0.25 }
]
Rules:
ALWAYS 3–5 scenes
ALWAYS sum to 1.0
NEVER “no scene”
NEVER confidence gating
Supported scene universe (MANDATORY COVERAGE)

System must map ANY input into at least 1–3 of:

🚗 Travel / Movement
road_trip
night_drive
highway_cruise
train_journey
flying
🌆 Urban / Night
city_night
neon_city
club
late_night
rooftop
🌄 Nature / Outdoor
countryside
desert_drive
forest_walk
beach_sunset
mountain
🕰️ Eras
60s_classic
70s_soul
80s_synth
90s_throwback
2000s_pop
modern
🎭 Emotions
nostalgia
melancholy
euphoric
angry
calm
romantic
⚡ Activities
workout
studying
partying
driving
cooking
gaming
2. GENRE SYSTEM (POST-SCORING ONLY)
Genres are NEVER used in scoring.

Only used AFTER ranking.

Post-process step:

After top 200 are ranked:

enforce:
maxGenreShare = 0.35
minGenres = 3
softBalanceTarget = true
If imbalance detected:

Instead of filtering out tracks:

reweight selection probability

NOT deletion.

3. SCORING ENGINE (PURE RANKING ONLY)
FINAL SCORE:
score =
  semantic * 0.45 +
  emotion  * 0.25 +
  scene    * 0.30
Scene score = vector similarity:
sceneScore =
  Σ(sceneVector[i].weight × trackSceneAffinity[i])
NO OTHER FACTORS:
no genre
no ecosystem
no filters
no caps
4. EMOTION ENGINE (STABLE LAYER)

Must ALWAYS output:

emotionVector = {
  valence: -1 to +1,
  energy: 0 to 1,
  intensity: 0 to 1
}

Used only in semantic weighting — never as filter.

5. POOL SYSTEM (NO FILTERING EVER)
Replace ALL of this:
pool caps
pre-filtering
ecosystem pruning
adjacency filtering
With:
NOTHING

All tracks enter scoring.

6. DIVERSITY ENGINE (CRITICAL FIX)

This is what your system was missing.

After ranking:

Step 1 — cluster results:
by genre
by energy
by era tag
Step 2 — enforce soft distribution:
targetDistribution = {
  dominant: 0.40,
  secondary: 0.40,
  exploration: 0.20
}
Step 3 — sampling not filtering:

You do NOT remove tracks.

You adjust selection probability:

finalWeight =
  score * diversityBoost(cluster) * noveltyFactor
7. FALLBACK LOGIC (THIS FIXES YOUR “GENERIC MODE” BUG)
NEVER DO:
“no scene matched → generic mode”
INSTEAD:

If weak input:

expand scene entropy:
if (sceneConfidence < 0.5) {
  increaseSceneSpread()
}

Meaning:

more scenes included
NOT fallback
8. WHAT THIS FIXES IN YOUR SYSTEM
Before (your current issue):
country dominates
indie collapse
“generic fallback”
scene fails → system collapses
heavy genre locking
After V10:

You get:

“90s throwbacks”

→ blended:

alt rock 90s
hip hop 90s
pop 90s
soft indie 2000 crossover
“cowboy dirt roads”

→ blended:

country core
folk rock
indie americana
soft rock
blues crossover
“chill study vibes”

→ blended:

ambient electronic
lo-fi hip hop
soft indie
cinematic piano
ANY input works because:
nothing is ever “invalid”
nothing is ever “blocked”
everything is redistributed instead of deleted
9. WHY THIS IS YOUR TRUE “FINAL VERSION”

Because it removes the real cause of all your previous failures:

❌ Old system:
filtering fights scoring
rules fight diversity
scene fights genre
everything collapses under edge inputs
✅ V10 system:
scoring is pure ranking
diversity is post-processing
scene is probabilistic context
genre is only shaping, never blocking
If you implement ONLY ONE THING:

Implement this rule:

“NEVER REMOVE TRACKS BEFORE FINAL SELECTION”

That alone fixes ~70% of your current instability.