---
description: Locked hero illustrations — 5 soft Pets-style objects only
globs:
  - frontend/public/index.html
  - frontend/public/cinema/**
  - docs/**/*
alwaysApply: false
---

# Locked hero objects (5)

Read `docs/KWALIFY_HERO_OBJECTS_LOCKED.md` and `docs/KWALIFY_OBJECT_CONSTRUCTION_MODE.md` before any hero or mood card work.

Allowed `illust` keys only: `pump` `road` `lamp` `car` `horizon`

Pump SVG: `docs/KWALIFY_PETROL_PUMP_SVG_LOCKED.md` (6 layers: body → head → screen → hose → nozzle → accent)

Each emotion screen:
- ONE soft illustrated hero (centered)
- Soft gradient background only
- NO environments, extra objects, or dashboard chrome

Code: `PETS_ILLUST_DEFS` in `frontend/public/index.html`

FAILSAFE: If industrial/diagram/icon UI or blob chaos → STOP. Replace with soft Pets-style illustration from locked spec.

Do not add heroes without updating locked doc + `EMOTION_MOODS`.
