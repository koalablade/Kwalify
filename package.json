{
  "name": "kwalify",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install --prefix artifacts/api-server --include=dev && npm run build --prefix artifacts/api-server",
    "start": "node artifacts/api-server/dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "overrides": {
    "typescript": "5.7.3"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
