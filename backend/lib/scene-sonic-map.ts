/**
 * Scene → sonic mapping (audio structure, not just emotion numbers).
 */

export {
  type SonicProfile,
  getSonicProfile,
  applySonicProfileToEmotion,
  sonicFitBonus,
  SONIC_BY_CANONICAL,
} from "./scene-sonic-profile";
