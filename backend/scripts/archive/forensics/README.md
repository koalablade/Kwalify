# Archived forensics scripts

One-off pipeline investigation tools. **Not used in production or CI.**

Run from repo root:

```bash
npx tsx backend/scripts/archive/forensics/<script>.ts
```

Moved from `backend/scripts/` to keep the main scripts folder maintainable.
