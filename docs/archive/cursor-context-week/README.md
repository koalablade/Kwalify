# Cursor context archive (last 7 days)

Archived automatically on 2026-07-25 during disk cleanup.

Contains:
- Agent transcript JSONL files from the past week (Kwalify project)
- Recent benchmark state snapshots (`benchmark-history.json`, etc.)

This folder is local backup only — not required for the app to run.
To search old conversations, grep these `.jsonl` files or open them in a text editor.
